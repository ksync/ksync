# Binaries for programs and plugins
*.exe
*.dll
*.so
*.dylib

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# dep vendor packages
vendor

bin
rootfs/radar

.DS_Store

*.pyc
__pycache__

# Using scss, css is a build artifact that gets placed everywhere.
*.css

.vscode
